# Copy: cp .env.example .env
# Source operational database (Airbyte reads from here via de_poc_network).
POSTGRES_USER=your_source_db_user
POSTGRES_PASSWORD=your_source_db_password_here
POSTGRES_DB=sales_source
