# Copy to .env and set your values. Safe to commit (no real secrets).
# Maps to POSTGRES_* consumed by the postgres:16-alpine image.
POSTGRES_USER=your_source_db_user
POSTGRES_PASSWORD=your_source_db_password_here
POSTGRES_DB=sales_source
