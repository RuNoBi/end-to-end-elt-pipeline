# Copy: cp .env.example .env
# Generate Fernet: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# --- Airflow internal metadata database (not the warehouse) ---
POSTGRES_USER=airflow
POSTGRES_PASSWORD=your_airflow_metadata_password_here
POSTGRES_DB=airflow

AIRFLOW__CORE__FERNET_KEY=your_fernet_key_here

AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=your_airflow_ui_password_here
AIRFLOW_ADMIN_EMAIL=admin@example.com
AIRFLOW_ADMIN_FIRSTNAME=Admin
AIRFLOW_ADMIN_LASTNAME=User

# --- Airbyte OSS 0.63 API ---
AIRFLOW_VAR_AIRBYTE_CONNECTION_ID=your_airbyte_connection_uuid_here
AIRFLOW_VAR_AIRBYTE_API_BASE_URL=http://airbyte-proxy:8000/api/v1

# --- dbt (must match warehouse-postgres/.env) ---
DBT_WAREHOUSE_HOST=de_poc_warehouse_postgres
DBT_WAREHOUSE_PORT=5432
DBT_WAREHOUSE_USER=your_warehouse_db_user
DBT_WAREHOUSE_PASSWORD=your_warehouse_db_password_here
DBT_WAREHOUSE_DB=data_warehouse
DBT_WAREHOUSE_SCHEMA=gold
DBT_THREADS=4

# --- Failure email alerts (Mailpit — docs/AIRFLOW_ALERTING.md) ---
AIRFLOW_ALERT_EMAILS=your_email@example.com
AIRFLOW_WEBSERVER_BASE_URL=http://localhost:8080
AIRFLOW_SMTP_HOST=airflow-mailpit
AIRFLOW_SMTP_PORT=1025
AIRFLOW_SMTP_USER=
AIRFLOW_SMTP_PASSWORD=
AIRFLOW_SMTP_MAIL_FROM=elt-alerts@de-poc.local
AIRFLOW_SMTP_STARTTLS=False
AIRFLOW_SMTP_SSL=False

# --- CKAN publication (run ckan-platform/scripts/bootstrap-ckan.sh first) ---
CKAN_URL=http://ckan:5000
CKAN_API_TOKEN=your_ckan_api_token_from_bootstrap_here
AIRFLOW_VAR_CKAN_API_TOKEN=your_ckan_api_token_from_bootstrap_here
CKAN_ORGANIZATION=ube-group-thailand
AIRFLOW_VAR_CKAN_ORGANIZATION=ube-group-thailand
CKAN_PUBLISH_MAX_ROWS=100000
AIRFLOW_VAR_CKAN_PUBLISH_MAX_ROWS=100000
CKAN_UPSERT_BATCH_SIZE=2000
AIRFLOW_VAR_CKAN_UPSERT_BATCH_SIZE=2000
