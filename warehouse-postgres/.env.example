# Copy: cp .env.example .env
# Same user/password must be set in dbt-warehouse/.env and airflow-platform/.env (DBT_*).
POSTGRES_USER=your_warehouse_db_user
POSTGRES_PASSWORD=your_warehouse_db_password_here
POSTGRES_DB=data_warehouse
