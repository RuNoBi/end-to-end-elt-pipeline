# Copy: cp .env.example .env
# Use Docker network hostname (not localhost) when running dbt inside containers.
# DBeaver on Mac: localhost:5434 with same user/password/database.
DBT_WAREHOUSE_HOST=de_poc_warehouse_postgres
DBT_WAREHOUSE_PORT=5432
DBT_WAREHOUSE_USER=your_warehouse_db_user
DBT_WAREHOUSE_PASSWORD=your_warehouse_db_password_here
DBT_WAREHOUSE_DB=data_warehouse
DBT_WAREHOUSE_SCHEMA=gold_sales
DBT_TARGET=dev
DBT_THREADS=4
